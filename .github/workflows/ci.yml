name: CI
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node (example)
        run: |
          echo "Add language/tool setup here (node, python, go...)"
      - name: Run tests
        run: |
          echo "Replace this with your project's test command"

